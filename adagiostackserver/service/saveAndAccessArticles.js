const {store} = require('./mongoApi');

// Send text to Google language API for processing and identify
// entities.
// POST /save
module.exports.save = (req, res) => {
    store({
        title,
        content,
        result,
        client
    }, (data) => {

    }, (error) => {

    })
}

module.exports.load = (req, res) => {

}